import React from 'react';

import { FieldProps } from 'formik';

import { EventFormValues } from '../EventForm';

const EventTimeInput: React.FC<FieldProps<EventFormValues>> = () => {
  // const { values } = props.form;
  // const { field } = props;

  return <div>nice</div>;
};

export default EventTimeInput;
